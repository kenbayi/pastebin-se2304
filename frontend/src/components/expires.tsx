import React from 'react';

const Expires = () => {
    return (
        <div className="position-absolute top-50 start-50 translate-middle">
            <h2>This paste has expired.</h2>
            <p>You can no longer access its content.</p>
        </div>
    );
};

export default Expires;
